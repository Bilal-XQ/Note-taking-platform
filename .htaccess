RewriteEngine On
RewriteBase /main/

# Allow direct access to files (like CSS and JS)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule .* - [L]

# Redirect all other requests to index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L] 